Employee photos are stored in this folder by the ERP app — ENCRYPTED, named by employee code
(e.g. photos/C-10006.enc).

Do NOT delete these files. They can only be viewed by logged-in users inside the app.

বাংলা: কর্মীদের ছবি এনক্রিপ্ট করে কর্মী কোড নামে এই ফোল্ডারে জমা হয় (যেমন C-10006.enc)।
এই ফাইলগুলো মুছবেন না। শুধু লগইন করা ইউজাররা অ্যাপের ভেতরে ছবি দেখতে পারবে।
